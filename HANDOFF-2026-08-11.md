# Handoff — ZION（pi-martix-ui）会话交接

> 生成：2026-08-11 ｜ 会话性质：wayfinder 路线图完成 + React/Electron 脚手架落地 + 首个可运行闭环 E2E 验证
> 保存位置：`D:\pi-martix-ui-dev\HANDOFF-2026-08-11.md`（后续会话先读本文件 + AGENTS.md）

## 一、项目速览

**ZION**：以 **pi**（`@earendil-works/pi-coding-agent`）为底座的黑客帝国风 Windows 桌面编码 Agent —— 输入指令 → 真实 pi agent 会话流式执行 → Matrix 风 UI（数字雨/CRT）实时呈现。

| 仓库 | 路径 | 角色 |
|---|---|---|
| GitHub `elephanttalkheads/pi-martix-ui` | **`D:\pi-martix-ui-dev`**（开发/推送唯一目标） | 主仓，公开，MIT |
| Gitee `xuhuitalker/pi-martix-ui` | `D:\pi-martix-ui` | 备份/历史快照，勿开发 |

启动：`cd D:\pi-martix-ui-dev && npm run dev`（vite 起在 127.0.0.1:5173，Electron 窗口弹出）。
⚠️ 当前 dev 实例**未运行**；会话工作目录固定为 `D:\zion-workspace`（agent 实际操作的项目目录）。

## 二、决策已定案（详见 GitHub issues，勿重复讨论）

- 地图：`github.com/elephanttalkheads/pi-martix-ui/issues/1`（已关闭，9 张票全解）
- 决策链：#2 壳层(Electron 43) → #3 pi SDK 内嵌 → #4 RPC 后备 → #5 技术栈定案 → #6 分发(不签名/Gitee Releases+GitHub 镜像/NSIS+portable/开源) → #7 环境预检 → #8 开源落地(MIT/GitHub 主) → #9 UI 框架(React 18+zustand+vite)
- **技术栈一句话**：Electron 43.x（≥39）+ pi SDK 进程内 + React 18/zustand/vite 8 + preload IPC 桥（contextIsolation+sandbox on，凭据只留主进程）+ electron-builder(NSIS+portable)

## 三、当前状态（已完成 → 已验证）

1. **首个可运行闭环**（commit `75b438d`）：`createAgentSession`（复用 `~/.pi/agent` 配置）→ `session.subscribe(事件流)` → IPC → React feed 渲染；IPC 面：prompt/abort/steer/followUp/ping
2. **两个关键修复**（commit `a9c8859`）：① preload 必须 `.cjs`（sandbox 下 ESM preload 不注入）；② vite 必须 `server.host:'127.0.0.1'`（vite8 只绑 ::1 导致 wait-on 卡死）
3. **E2E 已实测通过**：真实 prompt → deepseek-v4-flash（~12s）→ 工具调用行 [OK] → 流式回显，`stopReason: stop`。E2E 手法：`electron . --remote-debugging-port=9222` + Node 全局 WebSocket 连 CDP `Runtime.evaluate(awaitPromise)` 驱动 `window.zion.prompt`（可复用作回归测试）
4. **AGENTS.md**（commit `2c2182d`）：技术栈/命令/架构/4 个已知坑/当前状态/约定 —— **新会话必读**
5. **ui-demo 升级 v3**（commit `a424bd5`，由另一 demo 会话产出）：深度分层数字雨+bloom / CRT 曲率抖动 / WebAudio 音效 / 蠕虫定位动画 / diff 修改卡；`ui-demo/index-v2.html` 保留为基线
6. **记忆已迁移**：hermes-memory 项目键 = git 目录名，`projects-memory/pi-martix-ui` 已复制到 `pi-martix-ui-dev` 键 → dev 会话自动带出全部项目决策记忆

## 四、已知坑（细节在 AGENTS.md「已知坑」节）

1. npm 11 allow-scripts 拦 electron 安装脚本 → `npm approve-scripts electron` 或 npmmirror 手动解压+写 path.txt
2. vite 8 IPv6 绑定 → `vite.config.mjs` 已固定 `host:'127.0.0.1'`，勿改回
3. sandbox 下 preload 必须 `.cjs`（CJS），新 preload 逻辑保持此约定
4. 本机 GitHub 域名可能被墙 → 下载一律走 npmmirror 镜像（`.npmrc` 已配）
5. `session.prompt()` 从不抛错 → 查末条消息 `stopReason`；`ModelRuntime` 刷新可能挂 → 初始化已有 45s 超时保护

## 五、下一步工作（按价值排序；承接 `pi-matrix-demo-handoff.md`）

1. **v3 视觉资产 → React renderer 迁移**（当前第一优先级）：按 `pi-matrix-demo-handoff.md` 的模块清单与 mock→真实映射表迁移；**开屏加载页（boot/CRT 亮线/红蓝药丸）明确不实现**；涉及数字雨分层/CRT 层/WebAudio 音效/蠕虫动画/diff 卡
2. **项目选择 UI**：现在 agent 会话 cwd 固定 `D:\zion-workspace`，需"选项目目录"入口（agent 才能对用户项目干活）；注意项目信任（project trust）headless 下 `ask` 会静默忽略项目资源，需显式处理
3. **工具调用行详情展开**：feed 里 tool 行可点开看 args/结果
4. **会话历史/恢复**：SessionManager.continueRecent/open + 会话列表 UI
5. **扩展 UI 桥**：`ctx.ui` 默认 headless，需自实现 `ExtensionUIContext`（select/confirm/input → 桌面 UI 弹窗），参考 tbrandenburg/pi-desktop 的 ui-context 实现
6. **多 agent 卡**（NEO/TRINITY/MORPHEUS = 多会话并行）—— 先验证并发 AgentSession 安全性（调研遗留 Gap）
7. 杂项：离线字体（Google Fonts → 本地打包）、Gitee 备份镜像配置（Gitee 网页「导入仓库」+自动同步，未做）、`npm run dist` 打包实测、频谱（音频可视化）是否接真音频

## 六、建议技能（suggested skills）

- **prototype** — v3 视觉迁移 / 面板交互原型（第一优先级任务的主要工作方式）
- **tdd** — 会话控制、IPC、feed 状态等逻辑功能开发
- **code-review** — 提交前按仓库标准自审
- **diagnosing-bugs** — 疑难运行时问题（参考本文件的 E2E 复现手法）
- **grilling + domain-modeling** — 下一个架构级决策（多会话语义、信任流 UX）前先对齐
- **mcp-scripting** — GitHub issue/仓库操作（建票、关票、查状态）
- **writing-for-agents** — 修改 AGENTS.md 时
- **wayfinder** — 出现新的路线级决策时（当前地图已关闭）

## 七、参考物（不重复内容，按需打开）

- 仓库内：`AGENTS.md`、`pi-matrix-demo-handoff.md`、`research/*.md`（壳层/pi-SDK/pi-RPC/风格参考）、`ui-demo/index-v3.html`、`ui-demo/brand-spec.md`、`README.md`
- GitHub issues：#1 地图（9 条决策）+ 各决策票的决议评论
- 外部参考实现：`github.com/tbrandenburg/pi-desktop`（Electron 43 + pi SDK + React/zustand/vite，本项目的架构蓝本）
- 本地 pi docs：`C:\Users\zyf\AppData\Local\nvm\v24.19.0\node_modules\@earendil-works\pi-coding-agent\docs\`（sdk.md / rpc.md / extensions.md）
- 环境变量备份与 PATH 事故恢复法：`D:\skills-guide\用户配置\环境变量备份与恢复记录-2026-08-11.md`（动环境变量前必读）

## 八、安全与凭据（已脱敏）

- **本文件不含任何密钥/token**。敏感项只存在于：`~/.pi/agent/auth.json`（模型 API key）、`D:\skills-guide\用户配置\环境变量备份\*.reg`（环境变量含 GITHUB_PAT/GITEE_TOKEN 等，已提示勿外传）
- gh CLI 已 OAuth 登录（凭据在 Windows keyring），无需在代码/文档中出现
- 渲染进程零凭据：contextIsolation+sandbox，凭据只在主进程与 `~/.pi/agent`；新增 IPC 时保持此边界
- 会话历史：本会话（wayfinder 地图+构建期）可用 `session_search` 检索，勿复制全文
